# date-pick 바이브 코딩 워크플로우

이 문서는 `date-pick` 프로젝트에서 현재 사용 중인 바이브 코딩 방식을 재사용 가능한 개발 워크플로우로 정리한 것이다. 여기서 말하는 스킬은 repo 코드가 아니라 Codex 작업을 나누고 진행하기 위한 프로세스 문서와 실행 절차를 뜻한다.

이 흐름은 "all in claude code" 계열 워크플로우에서 유용한 아이디어를 그대로 복제한 것이 아니라, 필요한 부분만 가져와 Codex에 맞게 정리하고 역할별 스킬로 분리한 방식이다. 핵심은 한 번에 감으로 밀어붙이는 것이 아니라, 이슈 단위로 맥락을 고정하고, 요구사항/결정/구조/구현 계획/실행/개선을 분리해 충돌과 재작업을 줄이는 데 있다.

## 기본 원칙

- 작업 단위는 GitHub issue로 잡는다.
- 문서는 issue별 디렉터리(`docs/<issue-number>-<short-slug>/`)에 둔다.
- 코드 구현 전에 필요한 만큼 요구사항, ADR, 아키텍처, 구현 계획을 먼저 정리한다.
- 구현은 문서화된 `implementation-plan-*.md`를 기준으로 진행한다.
- 문서에는 대화 중 오간 의사결정 과정을 장황하게 남기지 않는다. 모든 문서에는 현재 결론, 배경, 선택한 결정, 대안, 영향처럼 나중에 다시 읽어도 필요한 내용만 남긴다.
- 다른 작업자가 동시에 움직일 수 있으므로, 각 worker는 쓰기 범위를 명확히 제한한다.

## 스킬 사용 순서

기본 순서는 다음과 같다.

1. `requirements-planner`
2. `adr-manager`
3. `architecture-planner`
4. `implementation-planner`
5. `execute-implementation-plan`
6. `improve-workflow`

`requirements-planner`는 목표, 범위, 완료 조건, 비목표, 미해결 질문을 정리한다. 이 단계에서는 무엇을 만들지와 어디까지 만들지를 고정한다.

`adr-manager`는 나중에 다시 질문받을 수 있는 결정만 남긴다. ADR에는 의사결정 회의록이나 대화 과정을 쓰지 않고, 결정의 배경, 결정 내용, 고려한 대안, 영향만 남긴다. 즉, "왜 이런 결론이 되었는가"를 재검토할 수 있을 정도로만 기록한다.

`architecture-planner`는 모듈 경계, 데이터 흐름, 외부 연동, 배포 구조를 정리한다. Slack Bot, Notion knowledge base, OpenAI API, 서버 실행 환경처럼 시스템을 나누어 이해해야 하는 요소가 있을 때 사용한다.

`implementation-planner`는 실제 파일, 모듈, 테스트, 검증 명령으로 이어지는 낮은 수준의 작업 계획을 만든다. 실행자가 바로 따라 할 수 있을 만큼 구체적이어야 하지만, 코드 자체를 대신 쓰는 문서는 아니다.

`execute-implementation-plan`은 계획된 구현 단계를 수행한다. 구현 중 요구사항이나 구조가 바뀌면 해당 문서를 함께 갱신하고, 검증 결과를 남긴다.

`improve-workflow`는 이미 만들어진 기준선 이후 개선, 방향 전환, 리팩터링, 정책 변경이 생겼을 때 사용한다. 단순 진행 기록이 아니라 의미 있는 변화가 있을 때 기존 문서와 변경 이력을 맞추는 역할이다.

## ADR 작성 원칙

ADR은 결정의 흔적을 남기는 문서이지, 대화 로그가 아니다. 따라서 다음 네 가지를 중심으로 쓴다.

- 배경: 어떤 요구사항이나 제약 때문에 결정이 필요했는가
- 결정: 현재 선택한 방식은 무엇인가
- 대안: 고려했지만 선택하지 않은 방식은 무엇인가
- 영향: 이 결정이 구현, 운영, 비용, 테스트, 확장성에 어떤 영향을 주는가

대화 중 나온 시행착오, 임시 의견, 중간 추론은 ADR에 남기지 않는다. 요구사항 문서, 아키텍처 문서, 구현 계획도 같은 원칙을 따른다. 문서의 목적은 "그때 무슨 이야기를 했는지"가 아니라 "현재 무엇을 기준으로 작업해야 하는지"를 빠르게 복원하는 것이다.

## progress.md와 changelog.md

`progress.md`는 짧은 resume card다. 다음 세션이나 다른 worker가 이어받을 때 필요한 상태만 남긴다. 완료 로그를 길게 쌓는 문서가 아니며, 대략 다음 정도면 충분하다.

- 상태
- 최근 완료
- 다음 작업
- 막힌 점
- 기준 문서

`changelog.md`는 모든 구현 단계마다 갱신하지 않는다. 방향 전환, 요구사항 변경, ADR 변경, 아키텍처 변경, 테스트 전략 변경처럼 기준선 이후 의미 있는 변화가 있을 때만 사용한다. 사소한 작업 진행은 PR 본문, 커밋, issue 댓글, 또는 짧은 `progress.md`로 충분하다.

## 서브에이전트 운영 방식

이 프로젝트에서는 여러 에이전트가 동시에 움직일 수 있으므로, 코드 구현과 문서화를 분리하고 각 worker의 쓰기 범위를 제한한다.

예를 들어 한 에이전트는 앱 코드와 테스트를 구현하고, 다른 에이전트는 루트 문서나 issue 문서를 정리한다. 이때 문서 담당 worker는 코드, 패키지 설정, issue docs 등 다른 소유 범위를 건드리지 않는다. 반대로 구현 담당 worker도 문서 변경이 필요한 경우에만 관련 문서를 좁게 갱신한다.

이 방식의 목적은 병렬 작업 자체가 아니라 충돌을 줄이는 것이다. 같은 파일을 여러 에이전트가 동시에 수정하지 않게 하고, 다른 사람이 만든 변경을 되돌리지 않으며, 각자의 산출물이 어떤 역할을 하는지 명확히 한다.

## date-pick 적용 예시

현재 `date-pick`에서는 Issue 001 데이트봇 MVP를 기준으로 이 흐름을 적용하고 있다. 제품 방향은 개인용 데이트 관리 AI 시스템이며, 주요 구성은 다음과 같다.

- Slack Bot: 사용자가 데이트 기록을 남기고 추천을 요청하는 대화형 인터페이스
- Notion knowledge base: 다녀온 장소, 후보 장소, 취향, 기록을 쌓는 지식 기반
- OpenAI API: 저장된 기록과 현재 조건을 바탕으로 데이트 후보를 추천하는 추론 계층
- Oracle Free VM: 개인용 봇을 저비용으로 운영하기 위한 서버 환경

Issue 001에서는 Slack을 입출력 채널로 두고, Notion을 장기 기억 저장소로 사용하며, OpenAI API가 영화, 맛집, 축제, 이벤트, 날씨, 최근 패턴, 예산, 이동 편의 같은 신호를 조합해 추천하는 MVP를 목표로 한다.

이 예시에서도 워크플로우의 핵심은 같다. 먼저 issue로 범위를 고정하고, 요구사항을 정리한 뒤, 필요한 결정은 ADR에 결론 중심으로 남긴다. 이후 Slack Bot, Notion 연동, OpenAI 호출, Oracle Free VM 운영 구조를 아키텍처로 분리하고, 구현 계획을 만든 다음 실행한다. 구현이 끝난 뒤에는 검증 결과를 PR에 남기고, 다음 세션에 꼭 필요한 정보만 `progress.md`에 남긴다.

## 재사용 기준

이 워크플로우는 `date-pick`에만 묶인 감상문이 아니라, Codex로 작은 제품을 issue 단위로 꾸준히 만들 때 재사용할 수 있는 기본형이다. 다만 모든 단계가 항상 필요한 것은 아니다. 변경이 작고 결정이 단순하면 ADR이나 아키텍처 문서는 생략할 수 있다. 반대로 외부 API, 비용, 보안, 데이터 저장, 배포 방식처럼 나중에 되돌아볼 가능성이 큰 선택이 있으면 문서 단계를 생략하지 않는다.

좋은 바이브 코딩은 문서를 많이 쓰는 것이 아니라, 다음 작업자가 바로 이어받을 수 있을 만큼만 결론을 남기는 것이다. `date-pick`의 현재 방식은 그 기준에 맞춰 Codex 스킬을 역할별로 나누고, issue-driven 흐름 안에서 필요한 만큼만 문서화하는 쪽으로 정리되어 있다.
